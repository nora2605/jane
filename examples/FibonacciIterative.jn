fn fib(n: uint) -> uint {
    let a = 0
    let b = 1
    let i = 0
    while i < n {
        let temp = a
        a = b
        b = temp + b
        i = i + 1
    }
    return a;
}

fn main() {
    let -c n = 10
    let result = fib(n)
    Tty.WriteLn("The ${n}th Fibonacci number is ${result}")
}